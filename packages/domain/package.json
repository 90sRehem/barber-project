{
  "name": "@barber-project/domain",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@barber-project/notifications": "*",
    "@barber-project/validations": "*",
    "bcrypt": "^5.0.1",
    "shallow-equal-object": "^1.1.1"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0"
  },
  "scripts": {
    "test": "jest",
    "build": "rimraf ./dist && swc src --out-dir dist"
  }
}
